/**
 * Server-side crypto — DEPRECATED.
 *
 * The ephemeral relay architecture no longer stores encrypted data on the server.
 * The server only relays client-encrypted blobs without any server-side encryption.
 * This file is kept empty to avoid breaking any residual imports during migration.
 */
